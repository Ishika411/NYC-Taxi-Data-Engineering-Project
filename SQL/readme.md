### SQL

This folder contains Spark SQL scripts used for querying and managing Delta tables.

The scripts demonstrate operations such as querying, updating, deleting, restoring, and validating data in the Gold layer.
